import { LikeButtonDirective } from './friend-button.directive';

describe('FriendButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new LikeButtonDirective();
    expect(directive).toBeTruthy();
  });
});
